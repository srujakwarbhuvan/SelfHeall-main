/* ================================================================
   ui.js — Minimal UI utilities for the extension webview.
   Dashboard events are now handled in index.html inline scripts.
   ================================================================ */

// Placeholder — dashboard events from ws-client route here
const UI = {
  onRunStart: () => {},
  onStepStart: () => {},
  onStepPass: () => {},
  onStepFail: () => {},
  onHealStart: () => {},
  onHealReason: () => {},
  onHealDone: () => {},
  onRunDone: () => {},
  onFragilityScan: () => {},
  setWsState: () => {},
};
